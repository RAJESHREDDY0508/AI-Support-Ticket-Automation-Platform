FROM node:20-alpine

WORKDIR /app

# Placeholder for frontend
COPY package*.json ./
RUN npm init -y 2>/dev/null || true

CMD ["sh", "-c", "echo 'Frontend placeholder - awaiting implementation' && sleep infinity"]
