# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ticket_platform
POSTGRES_PORT=5432

# Zookeeper
ZOOKEEPER_PORT=2181

# Kafka
KAFKA_PORT=9092
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Database connection (for services - use postgres hostname in Docker network)
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/ticket_platform

# Frontend
FRONTEND_PORT=3000

# API Gateway (when implemented)
API_GATEWAY_PORT=8000

# Ticket Service
TICKET_SERVICE_PORT=8001

# AI Worker (Python) + internal ticket-service auth
AI_WORKER_METRICS_PORT=9090
KAFKA_GROUP_ID=ai-worker-v1
INTERNAL_API_TOKEN=change-me-in-production
